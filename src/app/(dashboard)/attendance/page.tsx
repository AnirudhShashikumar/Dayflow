import { format, startOfMonth, endOfMonth } from "date-fns";
import { Clock3, Download, PencilLine } from "lucide-react";
import { requireProfile } from "@/lib/auth/session";
import { createClient } from "@/lib/supabase/server";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { EmptyState } from "@/components/shared/empty-state";
import { AttendanceCard } from "@/features/attendance/attendance-card";
import { correctAttendance } from "@/features/attendance/actions";
import { formatMinutes } from "@/lib/utils";

const tones={present:"success",absent:"danger",half_day:"warning",leave:"info"} as const;
export default async function AttendancePage({searchParams}:{searchParams:Promise<{date?:string,status?:string}>}){const profile=await requireProfile();const params=await searchParams;const supabase=await createClient();const management=profile.role!=="employee";const today=format(new Date(),"yyyy-MM-dd");const from=params.date??format(startOfMonth(new Date()),"yyyy-MM-dd");const to=params.date??format(endOfMonth(new Date()),"yyyy-MM-dd");
  if(management){let query=supabase.from("attendance_records").select("id,work_date,check_in,check_out,total_minutes,status,source,modification_reason,employee:employee_profiles(employee_code,designation,profile:profiles(full_name),department:departments(name))").gte("work_date",from).lte("work_date",to).order("work_date",{ascending:false});if(params.status)query=query.eq("status",params.status);const{data:records}=await query;return <div className="space-y-6"><div className="flex flex-col justify-between gap-3 sm:flex-row sm:items-end"><div><h2 className="text-2xl font-bold">Organization attendance</h2><p className="text-muted mt-1">Review, filter, correct and export recorded workdays.</p></div><Button asChild variant="outline"><a href={`/api/attendance/export?from=${from}&to=${to}${params.status?`&status=${params.status}`:""}`}><Download className="size-4"/>Export CSV</a></Button></div><Card><CardContent className="pt-5"><form className="flex flex-wrap gap-3"><input name="date" type="date" defaultValue={params.date} className="h-10 rounded-xl border bg-[var(--surface)] px-3 text-sm"/><select name="status" defaultValue={params.status??""} className="h-10 rounded-xl border bg-[var(--surface)] px-3 text-sm"><option value="">All statuses</option><option value="present">Present</option><option value="absent">Absent</option><option value="half_day">Half-day</option><option value="leave">Leave</option></select><Button type="submit" variant="secondary">Apply filters</Button></form></CardContent></Card><Card className="overflow-hidden"><div className="overflow-x-auto"><table className="w-full min-w-[920px] text-left text-sm"><thead className="bg-[var(--surface-muted)] text-xs uppercase tracking-wide text-muted"><tr>{["Employee","Date","Check in","Check out","Duration","Status","Correction"].map(h=><th key={h} className="px-5 py-3 font-semibold">{h}</th>)}</tr></thead><tbody className="divide-y">{(records??[]).map(r=>{const e=Array.isArray(r.employee)?r.employee[0]:r.employee;const p=e?(Array.isArray(e.profile)?e.profile[0]:e.profile):null;return <tr key={r.id} className="hover:bg-[var(--surface-muted)]"><td className="px-5 py-4"><p className="font-semibold">{p?.full_name}</p><p className="text-muted text-xs">{e?.employee_code} · {e?.designation}</p></td><td className="px-5 py-4">{format(new Date(`${r.work_date}T00:00:00`),"d MMM yyyy")}</td><td className="px-5 py-4">{r.check_in?format(new Date(r.check_in),"h:mm a"):"—"}</td><td className="px-5 py-4">{r.check_out?format(new Date(r.check_out),"h:mm a"):<span className="text-amber-600">Incomplete</span>}</td><td className="px-5 py-4">{formatMinutes(r.total_minutes)}</td><td className="px-5 py-4"><Badge tone={tones[r.status as keyof typeof tones]}>{r.status.replace("_"," ")}</Badge></td><td className="px-5 py-4"><details><summary className="cursor-pointer list-none"><Button type="button" variant="ghost" size="sm"><PencilLine className="size-4"/>Correct</Button></summary><form action={correctAttendance} className="mt-2 grid w-72 gap-2 rounded-xl border bg-[var(--surface)] p-3 shadow-xl"><input type="hidden" name="recordId" value={r.id}/><input name="checkIn" type="datetime-local" defaultValue={r.check_in?format(new Date(r.check_in),"yyyy-MM-dd'T'HH:mm"):""} className="h-9 rounded-lg border bg-transparent px-2 text-xs"/><input name="checkOut" type="datetime-local" defaultValue={r.check_out?format(new Date(r.check_out),"yyyy-MM-dd'T'HH:mm"):""} className="h-9 rounded-lg border bg-transparent px-2 text-xs"/><select name="status" defaultValue={r.status} className="h-9 rounded-lg border bg-transparent px-2 text-xs"><option value="present">Present</option><option value="absent">Absent</option><option value="half_day">Half-day</option><option value="leave">Leave</option></select><input name="reason" required minLength={3} placeholder="Correction reason" className="h-9 rounded-lg border bg-transparent px-2 text-xs"/><Button size="sm">Save correction</Button></form></details></td></tr>})}</tbody></table></div>{!records?.length&&<div className="p-5"><EmptyState title="No attendance records" description="Adjust the filters or check back after employees record attendance."/></div>}</Card></div>}
  const{data:employee}=await supabase.from("employee_profiles").select("id").eq("profile_id",profile.id).single();const{data:records}=employee?await supabase.from("attendance_records").select("id,work_date,check_in,check_out,total_minutes,status,notes").eq("employee_id",employee.id).gte("work_date",from).lte("work_date",to).order("work_date",{ascending:false}):{data:[]};const todayRecord=records?.find(r=>r.work_date===today)??null;const total=(records??[]).reduce((s,r)=>s+r.total_minutes,0);const present=(records??[]).filter(r=>r.status==="present"||r.status==="half_day").length;return <div className="space-y-6"><div><h2 className="text-2xl font-bold">My attendance</h2><p className="text-muted mt-1">A reliable record of your working time.</p></div><div className="grid gap-4 lg:grid-cols-3"><AttendanceCard record={todayRecord} serverNow={new Date().getTime()}/><Card><CardContent className="pt-5"><Clock3 className="size-5 text-[var(--primary)]"/><p className="text-muted mt-5 text-sm">Hours this month</p><p className="mt-1 text-3xl font-bold">{formatMinutes(total)}</p></CardContent></Card><Card><CardContent className="pt-5"><p className="text-muted text-sm">Attendance rate</p><p className="mt-6 text-3xl font-bold">{records?.length?Math.round(present/records.length*100):0}%</p><p className="text-muted mt-1 text-xs">{present} of {records?.length??0} recorded days</p></CardContent></Card></div><Card className="overflow-hidden"><CardHeader><CardTitle>Daily history · {format(new Date(),"MMMM yyyy")}</CardTitle></CardHeader><div className="overflow-x-auto"><table className="w-full min-w-[650px] text-left text-sm"><thead className="bg-[var(--surface-muted)] text-muted"><tr>{["Date","Check in","Check out","Duration","Status"].map(h=><th key={h} className="px-5 py-3 text-xs font-semibold uppercase">{h}</th>)}</tr></thead><tbody className="divide-y">{(records??[]).map(r=><tr key={r.id}><td className="px-5 py-4 font-medium">{format(new Date(`${r.work_date}T00:00:00`),"EEE, d MMM")}</td><td className="px-5 py-4">{r.check_in?format(new Date(r.check_in),"h:mm a"):"—"}</td><td className="px-5 py-4">{r.check_out?format(new Date(r.check_out),"h:mm a"):"—"}</td><td className="px-5 py-4">{formatMinutes(r.total_minutes)}</td><td className="px-5 py-4"><Badge tone={tones[r.status as keyof typeof tones]}>{r.status.replace("_"," ")}</Badge></td></tr>)}</tbody></table></div></Card></div>}
