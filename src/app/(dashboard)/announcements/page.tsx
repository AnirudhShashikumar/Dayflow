import{format}from"date-fns";import{Megaphone}from"lucide-react";import{requireProfile}from"@/lib/auth/session";import{createClient}from"@/lib/supabase/server";import{Card,CardContent,CardHeader,CardTitle}from"@/components/ui/card";import{Button}from"@/components/ui/button";import{Badge}from"@/components/ui/badge";import{Input,Label,Textarea}from"@/components/ui/input";import{publishAnnouncement,toggleAnnouncement}from"./actions";
export default async function AnnouncementsPage(){await requireProfile(["hr","admin"]);const{data}=await(await createClient()).from("announcements").select("*").order("created_at",{ascending:false});return <div className="grid gap-6 lg:grid-cols-[.75fr_1.25fr]"><Card className="h-fit"><CardHeader><div><CardTitle>Publish announcement</CardTitle><p className="text-muted mt-1 text-sm">Broadcast a clear organization update.</p></div></CardHeader><CardContent><form action={publishAnnouncement} className="space-y-4"><div><Label>Title</Label><Input name="title" required maxLength={120}/></div><div><Label>Message</Label><Textarea name="message" required maxLength={2000}/></div><div className="grid grid-cols-2 gap-3"><div><Label>Priority</Label><select name="priority" className="h-11 w-full rounded-xl border bg-[var(--surface)] px-3"><option value="normal">Normal</option><option value="important">Important</option><option value="urgent">Urgent</option></select></div><div><Label>Audience</Label><select name="audience" className="h-11 w-full rounded-xl border bg-[var(--surface)] px-3"><option value="all">All employees</option></select></div></div><div className="grid grid-cols-2 gap-3"><div><Label>Publish date</Label><Input name="publishDate" type="date" defaultValue={format(new Date(),"yyyy-MM-dd")} required/></div><div><Label>Expiry date</Label><Input name="expiryDate" type="date"/></div></div><Button className="w-full"><Megaphone className="size-4"/>Publish</Button></form></CardContent></Card><div className="space-y-3">{(data??[]).map(a=><Card key={a.id}><CardContent className="pt-5"><div className="flex items-start justify-between gap-3"><div><div className="flex items-center gap-2"><h3 className="font-semibold">{a.title}</h3><Badge tone={a.priority==="urgent"?"danger":a.priority==="important"?"warning":"neutral"}>{a.priority}</Badge><Badge tone={a.active?"success":"neutral"}>{a.active?"active":"inactive"}</Badge></div><p className="text-muted mt-2 text-sm">{a.message}</p><p className="text-muted mt-3 text-xs">Published {format(new Date(`${a.publish_date}T00:00:00`),"d MMM yyyy")} · Audience: {a.audience}</p></div><form action={toggleAnnouncement}><input type="hidden" name="id" value={a.id}/><input type="hidden" name="active" value={String(!a.active)}/><Button size="sm" variant="outline">{a.active?"Deactivate":"Activate"}</Button></form></div></CardContent></Card>)}</div></div>}
