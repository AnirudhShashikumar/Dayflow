import{Bell,Lock,Palette,Settings2}from"lucide-react";import{requireProfile}from"@/lib/auth/session";import{createClient}from"@/lib/supabase/server";import{Card,CardContent,CardHeader,CardTitle}from"@/components/ui/card";import{Input,Label}from"@/components/ui/input";import{Button}from"@/components/ui/button";import{updateOrganization}from"./actions";
export default async function SettingsPage(){const p=await requireProfile();const{data:o}=await(await createClient()).from("organization_settings").select("*").limit(1).maybeSingle();return <div className="space-y-6"><div><h2 className="text-2xl font-bold">Settings</h2><p className="text-muted mt-1">Personal preferences and organization configuration.</p></div><div className="grid gap-4 lg:grid-cols-3">{[[Palette,"Appearance","Use the sun/moon switch in the top bar. Your preference is remembered."],[Bell,"Notifications","In-app notifications are enabled for HR workflows and announcements."],[Lock,"Security","Sessions use Supabase Auth and private data is protected by Row Level Security."]].map(([Icon,title,body])=>{const I=Icon as typeof Bell;return <Card key={String(title)}><CardContent className="pt-5"><I className="size-5 text-[var(--primary)]"/><h3 className="mt-4 font-semibold">{String(title)}</h3><p className="text-muted mt-1 text-sm">{String(body)}</p></CardContent></Card>})}</div>{p.role==="admin"&&<Card className="max-w-3xl"><CardHeader><div><CardTitle>Organization settings</CardTitle><p className="text-muted mt-1 text-sm">Administrator-only configuration.</p></div><Settings2 className="size-5 text-[var(--primary)]"/></CardHeader><CardContent><form action={updateOrganization} className="grid gap-4 sm:grid-cols-2"><div className="sm:col-span-2"><Label>Organization name</Label><Input name="name" defaultValue={o?.organization_name??"Dayflow Technologies"} required/></div><div><Label>Timezone</Label><Input name="timezone" defaultValue={o?.timezone??"Asia/Kolkata"} required/></div><div><Label>Currency</Label><select name="currency" defaultValue={o?.currency??"INR"} className="h-11 w-full rounded-xl border bg-[var(--surface)] px-3"><option value="INR">INR — Indian Rupee</option><option value="USD">USD — US Dollar</option><option value="EUR">EUR — Euro</option></select></div><div><Label>Workday starts</Label><Input name="workdayStart" type="time" defaultValue={String(o?.workday_start??"09:00").slice(0,5)}/></div><div><Label>Workday ends</Label><Input name="workdayEnd" type="time" defaultValue={String(o?.workday_end??"18:00").slice(0,5)}/></div><Button className="sm:col-span-2">Save organization settings</Button></form></CardContent></Card>}</div>}
