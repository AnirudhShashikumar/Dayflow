import { ResetForm } from "@/features/auth/auth-form";
export default function ResetPage() { return <><p className="mb-2 text-sm font-semibold text-[var(--primary)]">Secure your account</p><h2 className="text-3xl font-bold tracking-tight">Choose a new password</h2><p className="text-muted mb-8 mt-2">Use at least eight characters.</p><ResetForm /></>; }
