import { requireProfile } from "@/lib/auth/session";
import { createClient } from "@/lib/supabase/server";
export async function GET(request:Request){await requireProfile(["hr","admin"]);const u=new URL(request.url);const from=u.searchParams.get("from");const to=u.searchParams.get("to");let q=(await createClient()).from("attendance_records").select("work_date,check_in,check_out,total_minutes,status,source,employee:employee_profiles(employee_code,profile:profiles(full_name))").order("work_date",{ascending:false});if(from)q=q.gte("work_date",from);if(to)q=q.lte("work_date",to);if(u.searchParams.get("status"))q=q.eq("status",u.searchParams.get("status")!);const{data,error}=await q;if(error)return new Response(error.message,{status:500});const esc=(v:unknown)=>`"${String(v??"").replaceAll('"','""')}"`;const rows=(data??[]).map(r=>{const e=Array.isArray(r.employee)?r.employee[0]:r.employee;const p=e?(Array.isArray(e.profile)?e.profile[0]:e.profile):null;return[p?.full_name,e?.employee_code,r.work_date,r.check_in,r.check_out,r.total_minutes,r.status,r.source].map(esc).join(",")});return new Response(["Employee,Employee ID,Date,Check In,Check Out,Minutes,Status,Source",...rows].join("\n"),{headers:{"Content-Type":"text/csv","Content-Disposition":"attachment; filename=dayflow-attendance.csv"}})}
