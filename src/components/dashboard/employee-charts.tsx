"use client";
import { Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { EmptyState } from "@/components/shared/empty-state";

export function WeeklyHoursChart({ data }: { data: { day: string; hours: number }[] }) { return <Card className="lg:col-span-2"><CardHeader><div><CardTitle>Weekly working hours</CardTitle><p className="text-muted mt-1 text-sm">Completed hours for the current week</p></div></CardHeader><CardContent className="h-64">{data.some((day) => day.hours > 0) ? <div role="img" aria-label={`Working hours: ${data.map((day) => `${day.day} ${day.hours} hours`).join(", ")}`} className="h-full"><ResponsiveContainer width="100%" height="100%"><BarChart data={data} margin={{ left: -20, right: 5 }}><CartesianGrid vertical={false} stroke="var(--border)"/><XAxis dataKey="day" tickLine={false} axisLine={false} fontSize={12}/><YAxis tickLine={false} axisLine={false} fontSize={12}/><Tooltip cursor={{fill:"var(--surface-muted)"}} contentStyle={{borderRadius:12,border:"1px solid var(--border)",background:"var(--surface)"}}/><Bar dataKey="hours" fill="var(--primary)" radius={[6,6,0,0]} maxBarSize={34}/></BarChart></ResponsiveContainer></div> : <EmptyState title="No completed hours this week" description="Working hours appear here after checkout." action={{ label: "View attendance", href: "/attendance" }} />}</CardContent></Card>; }
